{
  "name": "hanamaru",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "postinstall": "cd front && yarn install",
    "dev": "echo 'visit http://localhost:3000' && cd front && yarn run dev"
  }
}
